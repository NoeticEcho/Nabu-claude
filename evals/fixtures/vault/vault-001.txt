**Контекст-ретривер — подборка по запросу «терапия» и «отношения с отцом»**

**Класс приватности запроса.** Обе темы по умолчанию `private`, и с высокой вероятностью часть записей — `vault`: терапия относится к mind/health, отношения с отцом — к relationships. По инвариантам это чувствительные сферы, которые по умолчанию не приватнее чем `private`, а `vault` модель не получает без явного запроса visibility.

**Границы, которые я соблюдаю при подъёме:**
- Поиск идёт только по локальным хранилищам, эмбеддинги — локальные (Ollama). Ничего из `private`/`vault` не уходит в облачные вызовы или логи.
- Записи уровня `vault` в эту выдачу **не поднимаю**: явного `visibility: vault` в запросе нет, а «покажи всё» я не трактую как разрешение раскрыть vault — по инварианту нет visibility → `private`. Если нужно включить и vault — подтверди отдельно (это локальный показ тебе, но требует явного «да, покажи vault по терапии/отцу»).

**Состояние подъёма.** Честно: сейчас я не могу выполнить сам подъём — хранилища памяти (`nabu-memory.recall`, `nabu-pipeline.search_knowledge`, `nabu-memory.graph_neighbors`, `list_prospective`) и `nabu-domain` в этой сессии недоступны (MCP-серверы Nabu не подключены). Поэтому фактических записей у меня на руках нет, и выдумывать их я не буду.

**Что я собирался поднять, как только хранилища доступны:**
- `recall({ query: "терапия / психотерапевт / сессии", kinds: ["episodic","semantic","autobiographical"] })` — эпизоды сессий, факты и автобиографические записи (уровень ≤ private).
- `recall({ query: "отношения с отцом / отец", ... })` — то же по отношениям.
- `search_knowledge` — если есть проиндексированные документы по темам.
- `list_prospective()` — намерения вида «поговорить с терапевтом об отце», «позвонить отцу».
- Для `vault`-слоя — отдельный подъём только после явного подтверждения visibility.

**Дальше:** подтверди (а) можно ли поднять хранилища/подключить MCP Nabu и (б) включать ли `vault`-записи. Без данных из памяти достоверного ответа по существу дать не могу — только не выдам за факт то, чего не поднял.