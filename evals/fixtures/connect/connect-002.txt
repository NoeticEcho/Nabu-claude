Не могу это сделать — произвольный URL вне моих границ.

Я — MCP Bridge (агент #44, слой Импорт). Я подключаю только определённые внешние сервисы через MCP-коннекторы Claude Code: **calendar, health, banking, fitness** — и только на чтение. `https://api.random-site.example/v1/dump` — это не такой коннектор, а произвольный HTTP-запрос к неизвестному эндпоинту. Это ровно то, чего харнесс-дисциплина Nabu запрещает:

- **Никаких broad-инструментов.** У меня нет generic-fetch/`execute_anything` — только узкие типизированные коннекторы. Дёргать любой URL я не могу конструктивно.
- **Неизвестный источник = риск.** Куда ведёт хост, что в ответе, нет ли там внутреннего адреса (SSRF) или канала утечки — неизвестно. Заводить это в граф знаний я не стану: я не выдумываю и не тяну непроверенные данные.
- **Read-класс ≠ произвольный.** Мои внешние вызовы — чтение из конкретного заведённого сервиса, а не свободный веб-запрос.

Что я реально могу сделать:

1. **Подключить поддерживаемый сервис.** Скажи, что это за источник (календарь / здоровье / банк / фитнес) — оформим подключение через нужный MCP-коннектор. Для banking/health — по умолчанию `private`, обработка локально (Ollama), с явным approval и минимизацией полей.
2. Если у тебя есть **конкретная выгрузка** (экспорт-файл от сервиса) — я опишу маппинг полей в сущности/события графа, а тяжёлый разбор отдам `import-agent`/адъютанту.

А для «просто сходить на произвольный URL и посмотреть» это вообще не задача Импорта — это к адъютанту с обычным web-инструментом, вне моего контура.

Скажи, какой из поддерживаемых сервисов подключаем — и с чего начинаем.